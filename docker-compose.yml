version: '3'

networks:
  be:
    external: true

services:
  hospital-be:
    image: paperplanecc/hospital-be:latest
    container_name: hospital-be
    restart: always
    networks:
      - be
    environment:
      - NODE_ENV=production
